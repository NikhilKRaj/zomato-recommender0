# Local dev: omit these — Vite proxies /api to localhost:8000

# Production (Vercel dashboard → Environment Variables):
# RAILWAY_API_URL=https://your-api.up.railway.app
#
# Also set on Railway (CORS_ORIGINS) so the browser can call the API:
# CORS_ORIGINS=["https://zomato-recommender0.vercel.app","https://zomato-recommender0-git-main-technocrat2.vercel.app"]
