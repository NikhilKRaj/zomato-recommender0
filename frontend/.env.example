# Local dev: omit this variable — Vite proxies /api to localhost:8000
# VITE_API_URL=/api

# Production (Vercel): set in Vercel dashboard → Environment Variables
# VITE_API_URL=https://your-api.up.railway.app
