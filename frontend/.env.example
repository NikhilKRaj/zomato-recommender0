# Local dev: omit these — Vite proxies /api to localhost:8000

# Production: update the Railway URL in frontend/vercel.json if your backend URL changes.
# Optional Vercel env var (for reference only — proxy is configured in vercel.json):
# RAILWAY_API_URL=https://web-production-0c816.up.railway.app
